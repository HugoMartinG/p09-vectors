#include<iostream>
#include<vector>
using namespace std;
float ReduceSum(vector<int>vector1);
int main(){
    vector<int>vector1;
    for (int j = 0; j < 4; i++){
        cin >> vector1[j];
    }
    return 0;
}
float ReduceSum(vector<int>vector1){
    float sum{0.0};
    for (int i = 0; i < vector1.size(); i++){
        res += vector1[i];
    }

    return sum;
}
